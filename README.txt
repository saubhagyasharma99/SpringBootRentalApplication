a. Update Maven to download all dependencies

b. Create a databae as per the name in application manager.

c. If photo upload folder doesn't come up automatically, we have to manually create it at same location.

d. Change admin from non-active (0) to active(1) for registration via database.

e. The error/pop-up messages codes have been commented/hidden for the underlying null pointer exception error in run time. However, it can be uncommented/added and run and it should run smoothly once dependecies are injected. 